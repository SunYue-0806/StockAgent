| 周次 / 阶段 | 日期与作息安排 | 核心开发与学习任务 (精准到每天) | 阶段产出与通关标准 |
| :--- | :--- | :--- | :--- |
| W1: 底层通信 | Day 1 (周五): 短工时 (2.5h)<br>Day 2 (周六): 大块时间 (6h)<br>Day 3 (周日): 大块时间 (6h)<br>Day 4 (周一): 长工时 (1.5h)<br>Day 5 (周二): 长工时 (1.5h) | • 环境初始化：对接 DeepSeek-Coder / Claude 原生 API。<br>• 流式传输：手搓异步流式传输，用 Python `async generator` 实现终端/IDE 打字机效果。<br>• Function Calling：手写文件读写与目录遍历的 Tools Schema。<br>• JSON 解析：观察并解析大模型返回的 `tool_calls` 原始 JSON 结构。<br>• 动态执行器：利用 Python `getattr` / 安全反射真正调用本地代码工具。 | 🎯 基础脚本通关：<br>纯 Python 脚本能接收“帮我看看当前目录下有哪些 Python 文件，并读取 main.py 的前 20 行”，并成功解析出正确的工具参数并返回结果。 |
| W2: 工具外壳 | Day 6 (周三): 短工时 (2.5h)<br>Day 7 (周四): 长工时 (1.5h)<br>Day 8 (周五): 短工时 (2.5h)<br>Day 9 (周六): 大块时间 (6h)<br>Day 10 (周日): 大块时间 (6h) | • 防御机制：手搓损坏 JSON/ToolCall 修复器，用正则与 AST 校验防御模型格式报错。<br>• 基础工具箱：编写 `read_file`、`edit_file_patch`、`list_dir`、`run_command`。<br>• ReAct 引擎：手搓单体代码引擎外壳，写出带安全边界的 `while True` 循环。<br>• Diff 算法：实现统一 Diff，采用 Search-and-Replace 或 Unified Diff 精准替换，拒绝全局重写。<br>• 联合调试：验收工具箱、代码修改 Patch 与 ReAct 引擎。 | 🎯 阶段一验收：<br>输入“把当前项目里的所有函数变量名重构并加上类型标注”，系统能自动定位文件、输出 Patch 修改代码，并自动写入文件。 |
| W3: 代码记忆 | Day 11 (周一): 长工时 (1.5h)<br>Day 12 (周二): 长工时 (1.5h)<br>Day 13 (周三): 短工时 (2.5h)<br>Day 14 (周四): 长工时 (1.5h)<br>Day 15 (周五): 短工时 (2.5h)<br>Day 16 (周六): 大块时间 (6h)<br>Day 17 (周日): 大块时间 (6h) | • Token 计算：使用 `tiktoken` 编写基于代码上下文的 Token 计算器。<br>• 代码压缩：剔除注释与空行，生成轻量级代码骨架。<br>• 滑动窗口：触发小模型生成“已修改文件与修改意图”的抽象总结。<br>• 动态注入：将代码修改历史摘要动态注入后续请求的 `system_prompt`。<br>• 持久化存储：配置 Redis/SQLite，设计会话级与项目级存储结构。<br>• 记忆管理：封装 `CodeMemoryManager` 类，内聚上下文裁剪与会话加载。 | 🎯 阶段二验收：<br>连续对话 20 轮以上，Token 消耗锁定在 8k 阈值内，且模型依然清楚第 1 轮修改了哪个文件的哪几个函数。 |
| W4: 代码 RAG | Day 18 (周一): 长工时 (1.5h)<br>Day 19 (周二): 长工时 (1.5h)<br>Day 20 (周三): 短工时 (2.5h)<br>Day 21 (周四): 长工时 (1.5h)<br>Day 22 (周五): 短工时 (2.5h)<br>Day 23 (周六): 大块时间 (6h)<br>Day 24 (周日): 大块时间 (6h) | • AST 解析：集成 `tree-sitter`，按类/函数语义切分代码，拒绝固定字数切分。<br>• Repo Map：生成全项目的符号与文件依赖拓扑树。<br>• 向量检索：本地搭建 ChromaDB/Faiss，实现代码切片 Embedding 向量化。<br>• 精确检索：手搓 Ripgrep / BM25，专门用于精确查找类名、变量名。<br>• 混合 RAG：融合向量与符号检索，手写 RRF 重排算法。<br>• 工具封装：对接 RAG 到 Agent，封装为 `code_search` 与 `get_symbol_definition`。 | 🎯 阶段三验收：<br>完成高精 Code RAG 管道。输入“项目中哪里的全局中间件在处理 JWT Token 校验？”，能精准召回对应文件与行号。 |
| W5: 自愈图谱 | Day 25 (周一): 长工时 (1.5h)<br>Day 26 (周二): 长工时 (1.5h)<br>Day 27 (周三): 短工时 (2.5h)<br>Day 28 (周四): 长工时 (1.5h)<br>Day 29 (周五): 短工时 (2.5h)<br>Day 30 (周六): 大块时间 (6h)<br>Day 31 (周日): 大块时间 (6h) | • 防幻觉：解决 RAG 召回与直接修改结合时的“幻觉覆盖”与 Prompt 膨胀。<br>• 自愈循环：修改代码后自动触发终端 `pytest` / `mypy` 校验，抓取 Traceback 报错栈做二次修复。<br>• 多 Agent 拓扑：规划 Planner（架构师）、Coder（编码员）、Tester（测试自愈员）三大节点。<br>• 行为规范：为三大专家定制严苛的 `system_prompt` 与代码输出限制。<br>• 状态机引擎：纯 Python 手搓状态机，定义全局 `CodeProjectState` 字典。<br>• 条件路由：根据测试是否通过决定提交还是退回重改。 | 🎯 阶段三完工：<br>多 Agent 状态机骨架代码用纯 Python 编写完毕。具备“代码修改 ➔ 自动跑单测 ➔ 报错自动修复”的闭环能力。 |
| W6: 服务上线 | Day 32 (周一): 长工时 (1.5h)<br>Day 33 (周二): 长工时 (1.5h)<br>Day 34 (周三): 短工时 (2.5h)<br>Day 35 (周四): 长工时 (1.5h)<br>Day 36 (周五): 短工时 (2.5h)<br>Day 37 (周六): 大块时间 (6h)<br>Day 38 (周日): 大块时间 (6h) | • 安全沙箱：限制终端命令执行权限，防止危险指令。<br>• 熔断机制：加上最大迭代步数限制（Max Steps）与死循环熔断。<br>• Trace 追踪：在控制台/日志中打印漂亮的 Tool/AST/Fix 彩化日志。<br>• 端到端总测：在中型开源 Python/TS 项目中部署 Agent 进行 Bug 修复测试。<br>• API 封装：使用 FastAPI 封装 Agent 管道，提供 CLI / IDE 插件接入所需的异步 API。<br>• 实时推送：引入 SSE / WebSockets，向客户端实时推送思考状态、Diff 增量与终端输出。<br>• 性能优化：引入 Context Caching 降低 60%+ 的 Token 延迟。 | 🎯 系统全链路完工：<br>整个 AI 编码助手后端完工，支持 CLI/IDE 调用，具备“规划-写代码-跑单测-自愈-输出 Diff”的全流程能力。 |
| W7: 开源面试 | Day 39 (周一): 长工时 (1.5h)<br>Day 40 (周二): 长工时 (1.5h)<br>Day 41 (周三): 短工时 (2.5h)<br>Day 42 (周四): 长工时 (1.5h) | • 开源规范：推 GitHub，编写带架构图、AST 处理流程图与 Benchmarks 的顶级 README。<br>• 简历雕琢：用大厂 STAR 原则把“代码生成采纳率”、“自愈成功率”、“Token 成本下降 %”写进 Bullet。<br>• 模拟拷问一：深挖自研状态机、代码 AST 粒度 RAG 与不选 LangChain/LlamaIndex 的底层原因。<br>• 模拟拷问二：面对万行大型项目，如何处理 Context Window 溢出与 Patch 冲突。 | 🏆 全面通关：<br>带着硬核开源 AI Coder 项目与完美的 STAR 简历，向 AI Agent / LLM 应用架构师岗位发起冲刺！ |


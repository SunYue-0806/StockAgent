## 📅 42天在职手搓金融 Agent 每日学习与开发打卡表

| 周次 | 天数/日期 | 作息类型 (可用工时) | 核心开发与学习任务 (精准到每天干嘛) | 阶段产出与通关标准 |
| :--- | :--- | :--- | :--- | :--- |
| **W1**<br>底层<br>通信 | **Day 1 (周五)**<br>**Day 2 (周六)**<br>**Day 3 (周日)**<br>**Day 4 (周一)**<br>**Day 5 (周二)** | 短工时 (2.5h)<br>大块时间 (6h)<br>大块时间 (6h)<br>长工时 (1.5h)<br>长工时 (1.5h) | • 环境初始化，调通原生 API 的基础 chat 接口。<br>• **手搓异步流式传输**，用生成器处理打字机效果。<br>• **深入 Function Calling**，手写白酒核心指标函数 Schema。<br>• 观察并解析大模型返回的 `tool_calls` 原始 JSON 结构。<br>• 编写动态执行器，利用 Python `getattr` 真正调用本地函数。 | 纯 Python 脚本能接收“帮我算一下营收100成本60的毛利率”，并成功解析出正确的工具参数。 |
| **W2**<br>单体<br>引擎 | **Day 6 (周三)**<br>**Day 7 (周四)**<br>**Day 8 (周五)**<br>**Day 9 (周六)**<br>**Day 10 (周日)** | 短工时 (2.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>大块时间 (6h)<br>大块时间 (6h) | • **手搓损坏 JSON 修复器**，用正则防御格式报错。<br>• 编写白酒基础工具箱（ROE杜邦拆解、负债率计算）。<br>• 开始手搓单体 ReAct 引擎外壳，写出 `while True` 循环。<br>• **完成 ReAct 结合**，把函数结果拼回上下文让模型反复思考。<br>• **阶段一验收**：联合调试工具箱、异常修复与 ReAct 引擎。 | **【阶段一验收】**：输入一段乱七八糟的财务请求，系统能自动触发多次工具计算并输出最终正确答案。 |
| **W3**<br>记忆<br>系统 | **Day 11 (周一)**<br>**Day 12 (周二)**<br>**Day 13 (周三)**<br>**Day 14 (周四)**<br>**Day 15 (周五)**<br>**Day 16 (周六)**<br>**Day 17 (周日)** | 长工时 (1.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>大块时间 (6h)<br>大块时间 (6h) | • 使用 `tiktoken` 编写当前上下文的 Token 计算器。<br>• **实现滑动窗口记忆**，自动切片淘汰过老的消息。<br>• **实现后台摘要记忆**，触发小模型生成历史事实摘要。<br>• 将生成的“摘要”动态注入到后续请求的 `system_prompt` 中。<br>• 本地安装配置 Redis/SQLite，设计会话持久化存储结构。<br>• **封装 `MemoryManager` 类**，把裁剪、压缩、加载逻辑内聚。<br>• **阶段二压测验收**：连续向系统灌入长篇财报新闻测试记忆。 | **【阶段二验收】**：连续对话 20 轮，Token 消耗死死锁定在 4k 阈值内，且模型依然记得最开始聊的是哪家白酒公司。 |
| **W4**<br>知识<br>库管 | **Day 18 (周一)**<br>**Day 19 (周二)**<br>**Day 20 (周三)**<br>**Day 21 (周四)**<br>**Day 22 (周五)**<br>**Day 23 (周六)**<br>**Day 24 (周日)** | 长工时 (1.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>大块时间 (6h)<br>大块时间 (6h) | • 下载白酒财报 PDF，用 `pdfplumber` 提取基础文本。<br>• **编写表格提取逻辑**，将财报三表转化为干净的 Markdown 表格。<br>• **手搓层次化切片**，利用正则按财报业务章节结构化切片。<br>• 实现父子切片（Parent-Child Chunking）关联逻辑。<br>• 本地搭建 ChromaDB，实现切片向量化并持久化存入。<br>• **手搓精准关键词检索 (BM25)**，专门用于对齐金融长尾词。<br>• **融合混合检索**，用 RRF 算法手写重排，融合向量与 BM25。 | 完成高精金融 RAG 管道。输入“求今世缘去年的应收账款”，能把财报中对应表格切片完整、干净地召回。 |
| **W5**<br>状态<br>图谱 | **Day 25 (周一)**<br>**Day 26 (周二)**<br>**Day 27 (周三)**<br>**Day 28 (周四)**<br>**Day 29 (周五)**<br>**Day 30 (周六)**<br>**Day 31 (周日)** | 长工时 (1.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>大块时间 (6h)<br>大块时间 (6h) | • 将 RAG 知识库与单体 Agent 对接，封装为 lookup 工具。<br>• 调试 RAG 结合后的 Prompt，解决“未找到数据”时的幻觉。<br>• **阶段三验收**：测试复杂财报指标的问答准确率。<br>• 规划多 Agent 拓扑，定义数据、计算、策略三大节点。<br>• 为三大专家节点定制极其严苛的 system_prompt 行为规范。<br>• **纯 Python 手搓状态机引擎**，定义全局 `GlobalState` 字典。<br>• **编写条件路由 (Conditional Router)**，实现状态自动流转。 | **【阶段三验收】**：知识库彻底打通。<br>同时，多 Agent 的状态机骨架代码用纯 Python 编写完毕。 |
| **W6**<br>联调<br>上线 | **Day 32 (周一)**<br>**Day 33 (周二)**<br>**Day 34 (周三)**<br>**Day 35 (周四)**<br>**Day 36 (周五)**<br>**Day 37 (周六)**<br>**Day 38 (周日)** | 长工时 (1.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>大块时间 (6h)<br>大块时间 (6h) | • **引入 Reflection（反思机制）**，指标不对自动打回重抓。<br>• 多 Agent 深度联调，加入最大迭代步数限制以防止死循环。<br>• **实现全链路 Trace 追踪**，在控制台打印漂亮的彩化日志。<br>• **全系统端到端总装总测**，灌入完整白酒企业进行分析。<br>• 使用 FastAPI 封装 Agent 管道，提供异步任务 POST 接口。<br>• **引入 WebSockets/SSE**，向前端流式推送 Agent 思考状态。<br>• 并发压测，记录 Redis 语义缓存命中率与 Latency 下降数据。 | **【系统全链路完工】**：整个多 Agent 投研系统完全跑通，支持高并发，且运行过程全链路透明可观测。 |
| **W7**<br>面试<br>冲刺 | **Day 39 (周一)**<br>**Day 40 (周二)**<br>**Day 41 (周三)**<br>**Day 42 (周四)** | 长工时 (1.5h)<br>长工时 (1.5h)<br>短工时 (2.5h)<br>长工时 (1.5h) | • 规范代码推 GitHub，编写带架构图的顶级 README。<br>• **简历金牌雕琢**，用大厂 STAR 原则把优化数据写进 Bullet。<br>• 模拟面试拷问一：深挖自研状态机和不选 LangChain 的原因。<br>• 模拟面试拷问二：面对万份财报，如何用 Celery 异步扩展架构。 | **【全面通关】**：带着硬核开源项目与完美的 STAR 简历，开始正式向阿里 P6 岗位发起内推投递。 |